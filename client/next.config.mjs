/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "img.freepik.com",
      "images.unsplash.com",
      "firebasestorage.googleapis.com",
    ],
  },
};

export default nextConfig;
